create table shots(
    player_id integer,
    player text,
    shot integer,
    round_n integer,
    par integer,
    hole integer,
    shot_type text,
    distance_before text,
    distance_after text,
    shot_length text,
    category_before text,
    category_after text,
    x integer,
    y integer
)
